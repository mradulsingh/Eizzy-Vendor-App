<?xml version="1.0" encoding="utf-8"?>

<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <android.support.design.widget.TabLayout
            android:id="@+id/settlement_tabs"
            fontPath="fonts/Montserrat-Regular.ttf"
            android:layout_width="match_parent"
            android:layout_height="?android:actionBarSize"
            android:background="@color/sandstorm"
            app:tabGravity="fill"
            app:tabIndicatorColor="@color/deepSkyBlue"
            app:tabIndicatorHeight="3dp"
            app:tabMode="fixed"
            app:tabTextAppearance="?android:attr/textAppearanceLargePopupMenu"
            app:tabTextColor="@color/oxfordBlue" />

        <com.android.aksiem.eizzy.view.AppViewPager
            android:id="@+id/viewpager"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:background="@color/colorBgScreen" />

    </LinearLayout>

</layout>